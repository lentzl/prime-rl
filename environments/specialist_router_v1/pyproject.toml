[project]
name = "specialist-router-v1"
version = "0.1.0"
description = "Single-turn outcome-scored public capability router."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["specialist_router_v1"]
